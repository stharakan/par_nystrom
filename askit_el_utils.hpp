#include "El.hpp"
#include "fks_ompNode.h"

/* 
 * Function used to set some data from a given fksData structure.
 * el_data should already be set to the appropriate size so that 
 * memory is alloted. 
 */
void setDistMatrix(askit::fksData data, El::DistMatrix<double> el_data);



